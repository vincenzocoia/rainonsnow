% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_rain_snow.R
\name{fit_joint_rain_snow_cells}
\alias{fit_joint_rain_snow_cells}
\title{Fit joint rainfall–snowmelt models for every spatial group in a table}
\usage{
fit_joint_rain_snow_cells(
  data,
  group_cols,
  rainfall_col = "rainfall_hourly",
  snowmelt_col = "snowmelt_hourly",
  marginal_rainfall = "empirical",
  marginal_snowmelt = "empirical",
  bicop_family_set = "parametric",
  bicop_controls = list(),
  min_obs = 40L,
  progress = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame containing paired hourly columns.}

\item{group_cols}{Character vector of grouping column names (\code{cell_id},
\code{x}, \code{y}, etc.).}

\item{rainfall_col, snowmelt_col}{Names of the rainfall and snowmelt columns.}

\item{marginal_rainfall, marginal_snowmelt}{Distribution \strong{family} names passed to
\code{\link[famish:fit_dst]{famish::fit_dst()}} (e.g. \code{"empirical"}, \code{"gamma"}, \code{"weibull"}).}

\item{bicop_family_set, bicop_controls}{Passed to \code{\link[rvinecopulib:bicop]{rvinecopulib::bicop()}}.}

\item{min_obs}{Minimum paired complete observations required to fit the
copula; otherwise \code{bicop} is \code{NULL}.}

\item{progress}{If \code{TRUE}, shows a terminal progress bar over grid cells (via
\code{\link[utils:txtProgressBar]{utils::txtProgressBar()}}).}

\item{verbose}{If \code{TRUE}, prints a short \code{\link[=message]{message()}} after each cell is
fitted (can be very chatty with many cells).}
}
\value{
A grouped-by-row data frame with the same grouping keys plus column
\code{joint} (list of \code{joint_rain_snow} objects).
}
\description{
Groups \code{data} (usually \code{cell_id} or \verb{(x, y)}), fits one \code{joint_rain_snow}
model per group using \code{fit_joint_rain_snow_cell()}, and returns a row per
group with a list column \code{joint}.
}
