% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enframe_at_events.R
\name{enframe_at_events}
\alias{enframe_at_events}
\title{Tabulate marginal return levels on a POT event-frequency axis}
\usage{
enframe_at_events(
  marginal,
  num_events_per_year,
  return_periods = rp_reporting()
)
}
\arguments{
\item{marginal}{A distribution object passed to \code{distionary::enframe_return()}
as its first argument.}

\item{num_events_per_year}{Estimated PoT rate for the cell (scalar):
expected peaks per calendar year.}

\item{return_periods}{Reporting return periods in \strong{years}, in order; defaults
to \code{\link[=rp_reporting]{rp_reporting()}}. Values are multiplied by \code{num_events_per_year} before
passing to \code{enframe_return()}, which names that column \code{return_period}
(event units).}
}
\value{
A tibble: columns from \code{enframe_return()} (including \code{return_period}
in event units and \code{return}), plus \code{return_period_years} and
\code{num_events_per_year}.
}
\description{
Calls \code{distionary::enframe_return()} at abscissae \code{return_periods * num_events_per_year}, i.e. return periods expressed in \strong{events} (per the
PoT rate), not calendar years. Adds columns \code{return_period_years} (the same
grid as \code{return_periods}) and \code{num_events_per_year} so downstream code can
filter or plot using reporting years while \code{return_period} stays event-scaled.
}
\examples{
\dontrun{
enframe_at_events(my_marginal, num_events_per_year = 4.1)
enframe_at_events(my_marginal, 4.1, return_periods = c(2, 10, 100))
}
}
\seealso{
\code{\link[=rp_reporting]{rp_reporting()}}
}
