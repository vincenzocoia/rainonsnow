% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{eval_return_numeric}
\alias{eval_return_numeric}
\title{Return levels by numerical CDF inversion}
\usage{
eval_return_numeric(distribution, at, lower = 0, n_grid = 4000L)
}
\arguments{
\item{distribution}{A distionary distribution (\code{dst}) with a working CDF.}

\item{at}{Numeric vector of return periods (same convention as
\code{distionary::eval_return()}; the event-frequency axis in
\code{scripts/5-runoff_marginals.r}).}

\item{lower}{Lower bracket for the root search (default \code{0}, appropriate for
non-negative runoff).}

\item{tol}{Root-finding tolerance.}
}
\value{
A numeric vector of return levels, one per element of \code{at}.
}
\description{
Drop-in replacement for \code{distionary::eval_return()} for distributions whose
quantile function is disabled in the installed distionary (non-continuous
mixtures "lacking structured support", e.g. the GP-tail marginal built by
\code{\link[=mix2]{mix2()}} on grafted distributions). A return level for period \code{r} is the
quantile at \code{p = 1 - 1/r}, found here by root-finding on the CDF (which
remains available) rather than the disabled quantile algorithm. Matches
\code{eval_return()} to numerical precision in the tail, where return levels lie.
}
\seealso{
\code{\link[=rp_marginal_curve]{rp_marginal_curve()}}
}
