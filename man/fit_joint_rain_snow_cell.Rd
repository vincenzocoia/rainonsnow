% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_rain_snow.R
\name{fit_joint_rain_snow_cell}
\alias{fit_joint_rain_snow_cell}
\title{Joint model for hourly rainfall and snowmelt at one grid cell}
\usage{
fit_joint_rain_snow_cell(
  rainfall_hourly,
  snowmelt_hourly,
  cell_id = NA_integer_,
  x = NA_real_,
  y = NA_real_,
  marginal_rainfall = "empirical",
  marginal_snowmelt = "empirical",
  bicop_family_set = "parametric",
  bicop_controls = list(),
  min_obs = 40L
)
}
\arguments{
\item{rainfall_hourly, snowmelt_hourly}{Paired numeric vectors (same length),
typically mm/h as in \code{scripts/2-tablify_spatial_eo.r}.}

\item{cell_id, x, y}{Optional coordinates / id stored in \code{meta} for book-keeping.}

\item{marginal_rainfall, marginal_snowmelt}{Distribution \strong{family} names passed to
\code{\link[famish:fit_dst]{famish::fit_dst()}} (e.g. \code{"empirical"}, \code{"gamma"}, \code{"weibull"}).}

\item{bicop_family_set, bicop_controls}{Passed to \code{\link[rvinecopulib:bicop]{rvinecopulib::bicop()}}.}

\item{min_obs}{Minimum paired complete observations required to fit the
copula; otherwise \code{bicop} is \code{NULL}.}
}
\value{
An object of class \code{joint_rain_snow}: a list with components
\code{marginal_rainfall}, \code{marginal_snowmelt}, \code{bicop} (may be \code{NULL}),
\code{meta} (includes \code{n_obs}, families, and optional cell fields).
}
\description{
Fits two \strong{famish} marginals (each a \code{distionary} distribution object) and links
them with a bivariate copula from \strong{rvinecopulib} (\code{bicop()}). Because the
probaverse stack does not yet represent multivariate distributions as a
single \code{dst}, the result is a bespoke list with S3 class \code{joint_rain_snow}.
}
\details{
Pseudo-observations are margin cdf scores from \code{distionary::eval_cdf()},
scaled to fall strictly inside \eqn{(0,1)^2} so \code{bicop()} accepts them.
}
\seealso{
\link{simulate_joint_rain_snow}
}
