% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dst_empirical_to_gp.R
\name{convert_emp_to_gp}
\alias{convert_emp_to_gp}
\title{Empirical distribution to generalized Pareto (excess model)}
\usage{
convert_emp_to_gp(dst)
}
\arguments{
\item{dst}{An empirical distribution: the object returned by
\code{\link[distionary:dst_empirical]{distionary::dst_empirical()}}.}

\item{threshold}{Optional fixed threshold \code{u}. Excesses are \code{outcomes - u}
for outcomes strictly greater than \code{u}. If \code{NULL}, \code{u} is chosen
automatically (see \code{min_tail_prob}).}

\item{min_tail_prob}{When \code{threshold} is \code{NULL}, the largest value \code{u} on
the support of the empirical distribution such that \code{P(X > u) >= min_tail_prob}. Smaller values keep a larger tail sample; larger values
force a higher threshold.}
}
\value{
A \code{\link[distionary:dst_gp]{distionary::dst_gp()}} distribution for the fitted scale and shape
(excesses above \code{u}). Attributes:
\itemize{
\item \code{threshold} — numeric, the threshold \code{u} used.
\item \code{tail_prob} — \code{sum(probs[outcomes > u])}, the empirical mass in the tail.
\item \code{n_outcomes} — number of distinct outcome values strictly above \code{u}.
}
}
\description{
Fits a generalized Pareto distribution to the upper tail of an empirical
distribution produced by \code{\link[distionary:dst_empirical]{distionary::dst_empirical()}}, using weighted
maximum likelihood on the discrete exceedance probabilities. The returned
\code{\link[distionary:dst_gp]{distionary::dst_gp()}} object describes \strong{excesses} \code{Y = X - u} conditional
on \code{X > u}, with support starting at \code{0}, matching \code{dst_gp}'s usual peak-over
threshold parameterization.
}
\examples{
set.seed(1)
y <- c(stats::rnorm(200), stats::rnorm(100, mean = 6, sd = 1.2))
emp <- distionary::dst_empirical(y)
gp <- dst_empirical_to_gp(emp, threshold = stats::quantile(y, 0.85))
attr(gp, "threshold")
gp
}
