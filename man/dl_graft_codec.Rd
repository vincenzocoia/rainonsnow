% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dl_graft_codec.R
\name{dl_graft_codec}
\alias{dl_graft_codec}
\title{Compact storage for grafted GP-tail predictions}
\description{
Grafted distributions from \code{\link[=fit_and_graft_gp]{fit_and_graft_gp()}} are large when stored as a
list column. This module stores only the GPD tail (\code{graft_of}, \code{gp_scale},
\code{gp_shape}) and rebuilds \code{distribution_gp} from \code{distribution_forest} when
needed.
}
\section{Which function to use}{

\itemize{
\item \strong{Save or load the predictions RDS} (scripts, apps): \code{\link[=dl_write_peak_hour_predictions]{dl_write_peak_hour_predictions()}}
and \code{\link[=dl_read_peak_hour_predictions]{dl_read_peak_hour_predictions()}}. The file on disk is always the
compact form (no \code{distribution_gp} column).
\item \strong{Already in memory, about to write}: \code{\link[=dl_encode_peak_hour_distributions]{dl_encode_peak_hour_distributions()}}
after fitting; diagnostics can run on the full tibble \emph{before} encoding.
\item \strong{Already loaded a compact tibble}: \code{\link[=dl_decode_peak_hour_distributions]{dl_decode_peak_hour_distributions()}}
before anything that needs \code{distribution_gp} (e.g. \code{\link[=mix2]{mix2()}}, \code{\link[=dl_pp_long]{dl_pp_long()}}).
}
}

